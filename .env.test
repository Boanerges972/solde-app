VITE_SUPABASE_URL=http://test.supabase.co
VITE_SUPABASE_KEY=test-anon-key
# Tests unitaires ciblent le chemin legacy (db.from mocké). Le chemin RPC
# (db.rpc) est validé en base (staging 11/11) + en prod réel.
VITE_USE_RPC=false
